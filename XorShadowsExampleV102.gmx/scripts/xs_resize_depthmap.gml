///xs_resize_depthmap(resolution)

/*
    This function resizes the depthmap resolution.
    The shadow map resolution is always a square. That's why there's only one argument.
*/
surface_resize(XSdepth,argument0,argument0);
